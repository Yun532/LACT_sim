# Pure optical tracing: no efficiency losses.
# All output hits keep relative_efficiency = 1.
constant_scale=1
# Uniform fallback when no per-facet reflectivity scale is supplied.
mirror_reflectivity_scale=1
