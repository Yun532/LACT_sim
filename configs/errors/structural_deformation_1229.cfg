# 1229 望远镜的仰角相关支架变形。
# mirror.config 仍然保留理想镜片布局；如果这里开启 structural_deformation，
# 就会按当前 telescope.pointing_el_deg 用 series 文件覆盖每片镜子的 center/normal。
random_seed=987654321
structural_deformation_config=../mirrors/mirror_1229_elevation_series.cfg
