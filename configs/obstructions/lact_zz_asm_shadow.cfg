# Simplified 2D shadow mask extracted from /Users/yun/Downloads/lact-zz_asm.stp.
#
# The full STEP file is not used at runtime. It was projected to the telescope
# local aperture frame with:
#   STEP X -> local x
#   STEP Z -> local y
#   STEP Y -> local z
#
# The mask is evaluated at the mirror-support plane z=-16 m before mirror
# intersection. It is intended as a first-order support-shadow model for
# parallel-light optical tests.
enabled=true
mask_csv=lact_zz_asm_shadow_mask.csv
plane_z_m=-16.0
