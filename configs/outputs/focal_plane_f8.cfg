# Nominal 8 m focal plane used by the real camera.
#
# The same plane can also be used as a virtual whiteboard, but in camera runs it
# defines the camera face position and the image u/v axes.
plane_point=0,0,-8
plane_normal=0,0,-1
plane_u_axis=-1,0,0
plane_v_axis=0,1,0
csv=mirror_1229_hits.csv
