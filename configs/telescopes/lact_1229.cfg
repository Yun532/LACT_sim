# Single-telescope metadata and components for the default mirror layout.
id=0
name=LACT_1229

# Global array-frame reference point. Current single-telescope optical tests
# still run in local mirror coordinates, so this is metadata for now.
position_m=0,0,0
coordinate_system=array

# Pointing convention: azimuth in the array x-y plane, elevation above horizon.
pointing_az_deg=0
pointing_el_deg=90

focal_length_m=8.0
effective_focal_length_m=8.1787

mirror.config=../mirrors/mirror_1229_imported.cfg
output.config=../outputs/whiteboard_f8.cfg
efficiency.config=../efficiency/ideal.cfg
error.config=../errors/ideal.cfg
